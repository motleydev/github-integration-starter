- name: default
  kind: postgres
  configuration:
    connection_info:
      database_url:
        from_env: UPDATE_ME
      isolation_level: read-committed
      use_prepared_statements: false
  tables: "!include data/tables/tables.yaml"
